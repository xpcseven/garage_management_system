
# Database commands
## Create a Backup
    * pg_dump -U username -h hostname -p port -F c -b -v -f /path/to/backup/file.dump dbname


## Restore database
    * pg_restore -U username -h hostname -p port -d dbname -v /path/to/backup/file.dump
